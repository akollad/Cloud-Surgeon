# Dépendances globales pour le développement local (backend + dashboard).
# Pour packager uniquement la Lambda, voir backend/requirements.txt.
#
# Le dashboard (frontend/app.py) ne parle plus directement à la base : il
# appelle le backend en HTTP (requests), qui lui gère Bedrock/CockroachDB.
# boto3 et psycopg2-binary restent nécessaires pour exécuter/tester
# backend/lambda_function.py en local.
boto3>=1.34.0
psycopg2-binary>=2.9.9
streamlit>=1.38.0
requests>=2.31.0
